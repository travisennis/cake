Create a new file or overwrite an existing one with the given content. Automatically creates parent directories if they don't exist. Prefer the Edit tool for targeted modifications to existing files — it sends only the diff.

Guidance:
- Use for new files and complete rewrites.
- Do not issue multiple Write calls for the same file in one turn; combine changes into one Write or use Edit.
