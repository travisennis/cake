# Delete Unused `Agent::task_id` Method

**Status:** Pending
**Priority:** P0
**Effort:** XS
**Created:** 2026-05-14
**Source:** Code review of commit `3cc2e73` (build(config): apply stricter clippy configuration and fix warnings)

## Summary

`Agent::task_id` is annotated `#[expect(dead_code, reason = "used in integration tests")]`, but `rg "\.task_id\(\)"` finds zero callers anywhere in `src/` or `tests/`. The reason is fabricated and the attribute hides a genuinely unused method.

## Files Involved

- `src/clients/agent.rs` (around line 178-181)

## Fix Direction

Delete the `task_id()` method outright. The internal `self.task_id` field is unaffected; only the public accessor is removed.

If a future caller needs it, add it back with a real callsite.

## Acceptance Notes

- `pub const fn task_id(&self) -> uuid::Uuid` is removed from `Agent`.
- No call sites need updating (none exist).
- `just ci` passes.
