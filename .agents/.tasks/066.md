# Encapsulate Agent Public Mutable Fields

**Source:** prompt.md item 20

**Priority:** P2

**Effort:** M

**Depends on:** 048 (e2e test as safety net)

**Problem:** `Agent` exposes mutable public fields such as `session_id`, `task_id`, `total_usage`, and `turn_count`. Tests and main mutate internals directly.

**Files involved:** `src/clients/agent.rs`, `src/main.rs`, tests

**Fix:** Add constructors, accessors, and explicit mutation methods. Update tests to use fixture builders rather than direct field mutation.

**Status:** Completed
