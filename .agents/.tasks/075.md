# Check Whole File or Trust UTF-8 for Edit Binary Detection

**Source:** prompt.md item 41

**Priority:** P2

**Effort:** S

**Problem:** The edit tool checks only the first 8 KiB for null bytes before reading the whole file as UTF-8. The partial binary check is inconsistent with the later full UTF-8 validation.

**Files involved:** `src/clients/tools/edit.rs`

**Fix:** Remove the partial null-byte scan or check the whole file. Ensure invalid UTF-8 remains rejected with a clear message.

**Status:** Pending
