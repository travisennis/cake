# Flatten format_api_error_body

**Source:** prompt.md item 26

**Priority:** P3

**Effort:** S

**Problem:** `format_api_error_body` uses nested closures and `map_or_else`, making a simple parse-and-fallback path hard to read.

**Files involved:** `src/clients/agent.rs`

**Fix:** Rewrite using straightforward `if let` or `match` blocks with early returns.

**Status:** Pending
