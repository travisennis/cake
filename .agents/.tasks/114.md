# Update conversation-types.md After to_api_input Refactor

**Source:** Review of commit `21fb640f74a3` (task 061)

**Priority:** P3

**Effort:** XS

**Problem:** `docs/design-docs/conversation-types.md` was partially updated in commit `21fb640f74a3` to introduce the `ResponsesApiInputItem` and `StreamRecord` headings, but it still names `to_api_input()` as a `ConversationItem` method in headings and prose. Since 061, `to_api_input()` is `#[cfg(test)]`-only and is not the production path; readers of the design doc will look for the method on the type and find a test-only shim. The Chat Completions section also still references "to_api_input" as if it were the canonical Responses producer.

**Files involved:** `docs/design-docs/conversation-types.md`

**Fix:** Replace remaining references to `to_api_input()` in the design doc with `ResponsesApiInputItem::from(&ConversationItem)` (or "the Responses API DTO conversion in `src/clients/types.rs`"). Make sure the heading hierarchy and the Chat Completions comparison section read consistently with the typed-DTO approach.

**Acceptance notes:** No remaining references to `to_api_input()` in the design doc except where explicitly describing it as a test helper. Markdown rendering is correct.

**Status:** Pending
