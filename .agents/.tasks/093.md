# Rename with_text_progress to Avoid Builder Semantics

**Source:** prompt.md item 73

**Priority:** P3

**Effort:** S

**Problem:** `with_text_progress` returns `(Agent, ProgressBar)`, unlike other `with_*` methods that return `Self`.

**Files involved:** `src/main.rs`

**Fix:** Rename to `attach_text_progress` or similar so the function name matches its behavior.

**Status:** Pending
