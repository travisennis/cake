# Extract Tool Execution Helpers from Agent send()

**Source:** src/clients/agent.rs review

**Problem:** `Agent::send()` handles orchestration, streaming, progress reporting, skill deduplication, and tool execution in one large method. The tool execution block has deeply nested control flow and repeats the same `execute_tool(...).await` fallback several times.

**Files involved:**
- `src/clients/agent.rs:351`
- `src/clients/agent.rs:433`

**Fix:** Extract helpers such as `execute_tool_output()` and `execute_tool_with_skill_dedup()` so `send()` only coordinates the agent loop. Keep behavior unchanged and remove the `too_many_lines` suppression if the method becomes short enough.

**Status:** Completed
