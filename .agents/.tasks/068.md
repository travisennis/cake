# Replace with_history Debug Assertion with Real Invariant

**Source:** prompt.md item 22

**Priority:** P2

**Effort:** M

**Problem:** `with_history` uses `debug_assert!` for an invariant that disappears in release builds.

**Files involved:** `src/clients/agent.rs`

**Fix:** Encode construction order in a builder or perform a runtime validation that returns an error. Update call sites accordingly.

**Status:** Pending
