# Simplify with_activated_skills()

**Source:** src/clients/agent.rs review

**Problem:** `with_activated_skills()` loops and clones each skill into the mutex, then stores a separate copy in `prior_skill_activations`. If the write-only field is removed, this method can be much simpler.

**Files involved:**
- `src/clients/agent.rs:175`

**Fix:** Replace the mutex contents directly with the provided `HashSet<String>` where possible. Keep poisoned-lock behavior deliberate and consistent with the rest of the agent code.

**Status:** Completed
