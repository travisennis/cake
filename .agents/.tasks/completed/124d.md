---
id: 124d
title: Replace Module-Wide `string_slice` Suppression in skills.rs
status: Completed
priority: P1
effort: S
exec_plan: -
depends_on: -
---
# Replace Module-Wide `string_slice` Suppression in skills.rs

**Status:** Completed
**Priority:** P1
**Effort:** S
**Created:** 2026-05-14
**Parent:** 124

## Summary

`src/config/skills.rs` has a top-of-file `#![expect(clippy::string_slice, reason = "all string indexing is on ASCII code points from find() results or known offsets")]`. Replace with localized per-site `#[expect]` attributes.

## Files Involved

- `src/config/skills.rs`

## Fix Direction

1. Remove the module-level `#![expect(clippy::string_slice, ...)]`.
2. Most slices in this file are frontmatter parsing on `---` ASCII delimiters. Wrap each in a narrow `#[expect(clippy::string_slice, reason = "boundary returned by find('---') in ASCII content")]` block, or refactor with `split_once`/`splitn` where it is cleaner.
3. Confirm `just clippy-strict` is clean.

## Acceptance Notes

- Module-level attribute removed.
- String slicing was replaced with `strip_prefix` and `split_once`; no local suppression remains necessary.
- `cargo test config::skills` passes.
- `just ci` passes.
