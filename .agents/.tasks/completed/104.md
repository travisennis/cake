---
id: 104
title: Add Structured Provider Header Configuration
status: Completed
priority: P3
effort: M
exec_plan: -
depends_on: 050 (Backend abstraction), 052 (Provider strategy)
---
# Add Structured Provider Header Configuration

**Source:** prompt.md item 12

**Priority:** P3

**Effort:** M

**Depends on:** 050 (Backend abstraction), 052 (Provider strategy)

**Problem:** OpenRouter headers are hardcoded into request builders instead of being driven by provider or settings configuration.

**Files involved:** `src/clients/chat_completions.rs`, `src/clients/responses.rs`, settings model

**Fix:** Add provider/header configuration to model settings or provider strategy. Ensure headers are sent only when appropriate.

**Implementation notes:**

- Added ADR 008 for the structured provider/header settings contract.
- Added `provider = "openrouter"` and `provider_headers` model settings, preserving base-URL inference for existing OpenRouter configs.
- Moved OpenRouter attribution values behind typed provider headers in `ProviderStrategy`; generic providers still receive no OpenRouter headers.
- Documented the settings shape in README, design docs, and the example settings file.
- Validation: `cargo check --tests`, `cargo test provider_strategy`, `cargo test config::settings`, `cargo test config::model`, `just ci`.

### Deslop compliance

- Root AGENTS.md: read from the task context supplied for this repository.
- Nested AGENTS.md: none found under changed paths.
- Task context: read `.agents/TASKS.md`, `.agents/.tasks/index.md`, and task 104.
- Plans: `.agents/PLANS.md` read; not applicable because this is an M task with no ExecPlan.
- Design docs: read `docs/design-docs/index.md` and updated `docs/design-docs/settings.md`.
- ADRs: read ADR guidance and added `docs/adr/008-structured-provider-headers.md`.
- ExecPlan: no active plan applies to task 104.
- Changed files and diff: reviewed `git diff --stat` and targeted changed-file diffs.
- Validation: focused checks and `just ci` passed.

**Status:** Completed
