---
id: 145
title: Add Platform Verification Guidance for cfg-Sensitive Changes
status: Completed
priority: P2
effort: M
labels: type:task, area:unknown
exec_plan: -
depends_on: -
updated: 2026-06-04T21:28:59-04:00
---
# Add Platform Verification Guidance for cfg-Sensitive Changes

**Status:** Completed
**Priority:** P2
**Effort:** M
**Created:** 2026-05-15
**Source:** Evaluation of session `dd27bc06-67ed-41f1-86bd-c4de65888fa4`
**ExecPlan:** -
**Depends on:** -

## Problem

Ticket 131 involved cfg-sensitive sandbox fields and explicitly asked to verify dropped `dead_code` on non-macOS/non-Linux paths or document platform verification. The session checked installed targets but did not compile the installed Linux target and could not verify unsupported-platform behavior.

Evidence:

- Session line 14: task required verifying non-macOS/non-Linux compile behavior.
- Session line 67: `rustup target list --installed` showed `x86_64-unknown-linux-gnu`.
- Session line 131 completed task notes said Linux paths remain consistent, but the session did not run `cargo check --target x86_64-unknown-linux-gnu`.

## Relevant Files

- `.agents/TASKS.md`
- AGENTS.md project verification instructions
- `.agents/skills/deslop/SKILL.md`
- Any prompt guidance around Rust cfg/platform changes

## Fix Direction

Add guidance for cfg-sensitive changes:

- If changed code includes `#[cfg]`, platform-specific modules, or target-specific behavior, run the narrowest feasible target checks for installed targets.
- If a target is unavailable, explicitly report the gap in the task notes and final response.
- For Linux-specific Rust modules on macOS, prefer `cargo check --target x86_64-unknown-linux-gnu` when that target is installed and dependencies allow it.

Keep the guidance pragmatic; not every task needs exhaustive cross-compilation.

## Acceptance Notes

- Guidance tells agents how to verify installed non-host targets for cfg-sensitive changes.
- Guidance requires documenting unverified platform paths when checks are infeasible.
- The update avoids forcing broad cross-platform checks on ordinary host-only changes.
- Documentation-only changes do not need full CI unless generated prompt snapshots or code change.

Completed 2026-06-05:

- Added cfg-sensitive and platform-specific Rust verification guidance to root `AGENTS.md`.
- Guidance covers installed non-host target checks, `just clippy-linux` for Linux-sensitive work from macOS when available, closest feasible `cargo check --target ...` fallback, and explicit documentation of unverified platform paths.
- Kept the change in root agent workflow guidance instead of `.agents/TASKS.md` or `.agents/skills/deslop/SKILL.md`, because those are managed by the ahm task system and may be overwritten by `ahm upgrade`.
- Verification: inspected the Markdown diff and confirmed no code, config, generated prompt snapshots, fixtures, or dependency files changed; full CI skipped under docs-only workflow guidance.
