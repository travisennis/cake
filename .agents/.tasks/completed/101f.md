---
id: 101f
title: Extract Inline Tests from settings.rs
status: Completed
priority: P3
effort: S
labels: type:refactor, area:config
exec_plan: -
depends_on: -
---
# Extract Inline Tests from settings.rs

**Parent:** 101 (Review Module Size and Ownership Boundaries)

**File:** `src/config/settings.rs` (1,508 total, 529 production, 979 test)

**Goal:** Move `#[cfg(test)] mod tests { ... }` block to sibling file.

**Method:** Replace the inline `mod tests` block with:
```rust
#[cfg(test)]
#[path = "settings_tests.rs"]
mod tests;
```

Create `src/config/settings_tests.rs` with the extracted test module content.

**~979 lines moved. `settings.rs` drops to ~529 lines.**

**Acceptance:** `just ci` passes. All tests pass with `cargo test settings`. Zero production code changes.
