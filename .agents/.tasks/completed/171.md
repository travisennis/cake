---
id: 171
title: Extract Inline Tests from Remaining Large Modules
status: Completed
priority: P3
effort: L
labels: type:refactor, area:agent, area:chat, area:tools, area:session, area:config, area:cli
exec_plan: .agents/exec-plans/completed/extract-inline-tests-remaining-modules.md
depends_on: -
updated: 2026-07-01T09:48:46-04:00
---
# Extract Inline Tests from Remaining Large Modules

> **Note:** Effort L task currently Pending without an ExecPlan. Body says "Create an ExecPlan before implementation." Create one before starting code changes.

**Created:** 2026-05-23
**Source:** `review.md` Issue 4

## Problem

Several large modules still combine production code with large inline `#[cfg(test)]` modules. The recent extracted-test work established a repeatable pattern, but large files remain harder to scan than necessary.

Current line counts from `wc -l`:

| File | Lines |
| ---- | ----- |
| `src/clients/agent.rs` | 2413 |
| `src/main.rs` | 1697 |
| `src/clients/responses.rs` | 1664 |
| `src/hooks.rs` | 1449 |
| `src/types/session.rs` | 1109 |
| `src/config/skills.rs` | 1065 |
| `src/clients/tools/bash.rs` | 1016 |

The review claimed all seven are flagged by `just lint-module-size`; the actual current linter output flags only a subset based on production-line and test-ratio thresholds. The underlying consistency debt is still valid: large inline test blocks should be extracted where doing so reduces production-file clutter without changing behavior.

## Relevant Files

- `src/clients/agent.rs`
- `src/main.rs`
- `src/clients/responses.rs`
- `src/hooks.rs`
- `src/types/session.rs`
- `src/config/skills.rs`
- `src/clients/tools/bash.rs`
- Existing extracted test modules for examples, such as `src/clients/chat_completions_tests.rs`

## Fix Direction

Create an ExecPlan before implementation because this is broad, multi-module refactoring work.

Extract inline tests into sibling `*_tests.rs` modules using the established `#[cfg(test)] #[path = "..."] mod tests;` pattern. Work in small batches and prefer zero production behavior changes.

Prioritize files currently reported by `just lint-module-size`, then consider the remaining large modules if extraction is straightforward.

## Acceptance Notes

Completed 2026-07-01 via ExecPlan `.agents/exec-plans/completed/extract-inline-tests-remaining-modules.md`.

- ExecPlan authored before code changes; it scoped extraction order (responses, session, skills, bash, hooks, main) and validation strategy.
- Inline `#[cfg(test)] mod tests` blocks extracted into sibling `*_tests.rs` files via the established `#[cfg(test)] #[path = "..."] mod tests;` pattern, with zero behavior change. 253 `#[test]` functions moved out of production files; 253 now live in the new test files; 0 remain inline; 0 dropped.
  - `src/clients/responses.rs` 1854 -> 474 (two modules -> `responses_tests.rs` + `responses_response_parsing_tests.rs`, kept separate to preserve insta snapshot paths).
  - `src/types/session.rs` 1165 -> 487 (`session_tests.rs`).
  - `src/config/skills.rs` 1065 -> 636 (`skills_tests.rs`).
  - `src/clients/tools/bash.rs` 1208 -> 524 (`bash_tests.rs`; two tiny `#[cfg(test)]` helpers left in place to avoid rewriting `super::` paths).
  - `src/hooks.rs` 1497 -> 865 (`hooks_tests.rs`; production-line flag remains, out of scope).
  - `src/main.rs` 1999 -> 1017 (`main_tests.rs`; test-only `Role` import relocated; production-line flag remains, out of scope).
- `cargo check --tests` passed after each batch; targeted per-module tests passed; full suite is 806 tests passing, identical to baseline.
- `cargo fmt`, `just lint-module-size`, `just lint-imports`, `just clippy-strict`, `just clippy-no-default-features` all pass. New `*_tests.rs` files were added to the `cargo-crap` exclude list in `scripts/cargo-crap.sh` (matching prior extractions).
- Caveat: `just ci`'s `check-coverage` gate reports one CRAP regression, `Agent::send` (Δ +0.3) in the untouched `src/clients/agent/agent_loop.rs`. This reproduces identically on a clean tree with all task changes stashed — it is pre-existing coverage nondeterminism against a stale `ci/cargo-crap-baseline.json`, not caused by this task. Baseline regeneration was intentionally left to a maintainer.

