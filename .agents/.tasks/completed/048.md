---
id: 048
title: Add End-to-End Agent Loop Test with Stub Backend
status: Completed
priority: P1
effort: L
exec_plan: -
depends_on: -
---
# Add End-to-End Agent Loop Test with Stub Backend

**Source:** prompt.md item 67

**Priority:** P1

**Effort:** L

**Problem:** The codebase lacks an integration test that exercises model request, tool call, tool result, and final assistant response through the agent loop.

**Files involved:** `tests/`, `src/clients/agent.rs`, backend modules

**Fix:** Add a stub or wiremock-backed backend test covering one full agent loop turn with a tool call. Use this as a safety net before larger refactors.

**Status:** Completed

**Completion notes (2026-05-08):**
- Added a wiremock-backed agent loop test that starts with a user message, receives a Responses API `function_call`, executes the `Read` tool, sends the `function_call_output` on the next provider request, and returns the final assistant message.
- The test also verifies accumulated usage, persisted in-memory history, and streamed JSON records for the tool call, tool output, and final response.
