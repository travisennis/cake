---
id: 101c
title: Convert bash_safety.rs to Subdirectory with Separated Parsing
status: Completed
priority: P3
effort: S
labels: type:refactor, area:tools
exec_plan: -
depends_on: -
---
# Convert bash_safety.rs to Subdirectory with Separated Parsing

**Parent:** 101 (Review Module Size and Ownership Boundaries)

**File:** `src/clients/tools/bash_safety.rs` (891 production lines)

**Goal:** Split into `bash_safety/` subdirectory.

**What moves:**

- `bash_safety/mod.rs` (~530 lines): Orchestrator (`validate_command_safety`, `blocked`) + safety check rules (`check_git_*`, `check_dangerous_rm`, `has_unsafe_message_flag`, `is_allowed_rm_target`)
- `bash_safety/parse.rs` (~360 lines): Shell parsing utilities (`split_segments`, `extract_inline_script`, `extract_command_substitutions`, `strip_shell_data`, `extract_dollar_paren`, `extract_backticks`, `normalize_whitespace`, `find_inline_shell_invocation`, `find_shell_token`, `is_shell_token_boundary`, `skip_to_after_flag`)

**No visibility changes needed — the parsing functions are called only from `mod.rs`.**

**Acceptance:** `just ci` passes. No behavioral change. All existing tests pass.
