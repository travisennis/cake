---
id: 126
title: Remove Test-Only `Agent::activated_skills` Accessor
status: Completed
priority: P1
effort: S
exec_plan: -
depends_on: From code review of 110/3cc2e73
---
# Remove Test-Only `Agent::activated_skills` Accessor

**Status:** Completed
**Priority:** P1
**Effort:** S
**Created:** 2026-05-14
**Source:** Code review of commit `3cc2e73` (build(config): apply stricter clippy configuration and fix warnings)

## Summary

`Agent::activated_skills` is a `pub fn` annotated `#[cfg(test)]`. Production never calls it; only two `main.rs` tests do. Either expose it as a real production API or stop pretending it is one.

The cleanest path is deletion: rewrite the two test assertions to verify the same behavior through a clearer surface (the `SkillActivations` accessor used internally, a session-record assertion, or a `pub(crate)` test-only helper that is unambiguously named as such).

## Files Involved

- `src/clients/agent.rs` (around line 266-276)
- `src/clients/agent.rs` (around `SkillActivations::active`, around line 57-60 — also `#[cfg(test)]`)
- `src/main.rs` lines 1694 and 1713 (existing test call sites)

## Fix Direction

1. Delete `Agent::activated_skills`.
2. Delete `SkillActivations::active` if no remaining caller exists.
3. Update the two `main.rs` tests to assert seeded skills through whichever path is cleanest:
   - Preferred: introduce a `#[cfg(test)] pub(crate) fn` on `Agent` named `test_active_skills` (or similar) that makes its test-only nature obvious, OR
   - Assert through the session storage by inspecting the seeded `SkillActivated` records.
4. Confirm both tests still cover the original behavior (real-skill present, fake-skill absent).

## Acceptance Notes

- `Agent::activated_skills` is removed.
- The two existing test assertions still validate skill seeding.
- `just ci` passes.
- No `pub fn` on `Agent` remains gated to `#[cfg(test)]` while looking like a public API.
