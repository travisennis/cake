# Rationalize Clippy Allow Policy

**Source:** prompt.md item 1 high-level and item 68

**Priority:** P3

**Effort:** M

**Depends on:** Resolve in the same PR as 107 (missing_docs lint policy) for consistency.

**Problem:** The project has strict lint settings but many local `allow` annotations, especially in tests and long functions. The intended policy is unclear.

**Files involved:** `Cargo.toml`, Rust source files

**Fix:** Document acceptable local allows, remove unnecessary production allows, and decide whether crate-root lint policy should mirror Cargo lint policy.

**Status:** Pending
