# Model Session Mode as a Typed Run Mode

**Source:** prompt.md items 10 and 59

**Priority:** P1

**Effort:** M

**Problem:** Session mode selection is a long `if/else` chain over raw CLI fields. CLI parsing, validation, and execution mode are conflated.

**Files involved:** `src/main.rs`

**Fix:** Parse CLI flags into a typed `SessionMode` or `RunSpec` before execution. Dispatch on the typed mode and keep restore/fork/new construction explicit.

**Status:** Pending
