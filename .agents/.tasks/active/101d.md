---
id: 101d
title: Extract Inline Tests from edit.rs
status: Pending
priority: P3
effort: S
labels: type:refactor, area:tools
exec_plan: -
depends_on: -
---
# Extract Inline Tests from edit.rs

**Parent:** 101 (Review Module Size and Ownership Boundaries)

**File:** `src/clients/tools/edit.rs` (1,811 total, 786 production, 1,025 test)

**Goal:** Move `#[cfg(test)] mod tests { ... }` block to sibling file.

**Method:** Replace the inline `mod tests` block with:
```rust
#[cfg(test)]
#[path = "edit_tests.rs"]
mod tests;
```

Create `src/clients/tools/edit_tests.rs` with the extracted test module content.

**~1,025 lines moved. `edit.rs` drops to ~786 lines.**

**Acceptance:** `just ci` passes. All tests pass with `cargo test edit`. Zero production code changes.
