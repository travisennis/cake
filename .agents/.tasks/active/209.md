---
id: 209
title: Centralize Config Directory Resolution and Honor XDG_CONFIG_HOME
status: Pending
priority: P3
effort: S
labels: type:refactor, area:config, area:cli
exec_plan: -
depends_on: -
created: 2026-06-09T20:33:56-04:00
---
# Centralize Config Directory Resolution and Honor XDG_CONFIG_HOME

# Centralize Config Directory Resolution and Honor XDG_CONFIG_HOME

**Created:** 2026-06-09
**Source:** Code review of `src/main.rs` and `src/config/settings.rs` (2026-06-09)

## Problem

The config directory `~/.config/cake` is independently hardcoded in two
places:

- `src/main.rs:761` — `dirs::home_dir().unwrap_or_else(...).join(".config").join("cake")`,
  used for system prompt resolution.
- `src/config/settings.rs:375` — the same construction for
  `settings.toml` loading.

Neither honors `$XDG_CONFIG_HOME`. If the two sites ever drift, the system
prompt and settings would load from different config roots. (`data_dir.rs`
also builds `~/.config/AGENTS.md` paths independently.)

## Relevant Files

- `src/main.rs` — config dir construction in `run`
- `src/config/settings.rs` — `SettingsLoader` global path
- `src/config/data_dir.rs` — XDG-adjacent AGENTS.md lookups
- `src/prompts/mod.rs` — consumes the config dir

## Fix Direction

Add a single `config_dir()` helper in the config module and use it from both
sites. Consider resolving via `$XDG_CONFIG_HOME` falling back to
`~/.config` (the `dirs` crate's `config_dir()` differs on macOS — it returns
`~/Library/Application Support` — so an explicit XDG-style helper preserving
current `~/.config/cake` behavior is likely the right call). Document the
resolution order; configuration shape is a compatibility surface, so keep
default behavior unchanged unless explicitly deciding otherwise.

## Acceptance Notes

- One helper owns config-dir resolution; both call sites use it.
- Existing `~/.config/cake` behavior is preserved by default.
- If XDG_CONFIG_HOME support is added, it is tested and documented.
- `just ci` passes.

