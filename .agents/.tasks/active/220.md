---
id: 220
title: Fix debug models change-risk regression
status: Pending
priority: P2
effort: S
labels: type:test, area:cli
exec_plan: -
depends_on: -
created: 2026-06-24T18:03:52-04:00
---
# Fix debug models change-risk regression

## Problem

CI still fails in the Coverage job's `Check change-risk regression` step after the false-positive cargo-crap exclusions were aligned. One remaining production regression is in the debug models command runner.

Current GitHub output from run `28116394203`:

- `DebugCommand::run` CRAP 5.0, delta +1.0, CC 5, coverage 92.3%

This is likely a small branch-coverage gap after adding JSON output.

## Relevant Files

- `src/cli/debug.rs`
- `tests/debug_models.rs`
- `ci/cargo-crap-baseline.json`

## Fix Direction

Add focused tests around `DebugCommand::run` behavior for JSON and table output, including settings-load error behavior if that is the uncovered branch. Prefer exercising the public CLI or command runner path already used by existing tests.

Do not reduce coverage thresholds or exclude this function from cargo-crap analysis.

## Acceptance Notes

- The cargo-crap CI command no longer reports `DebugCommand::run`.
- Existing debug models behavior remains stable.
- `just ci` passes locally or any remaining failure is unrelated and documented with exact output.

