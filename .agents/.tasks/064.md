# Refactor Chat build_messages State Handling

**Source:** prompt.md item 13

**Priority:** P2

**Effort:** L

**ExecPlan:** `.agents/exec-plans/completed/refactor-chat-build-messages.md`

**Depends on:** 050 (Backend abstraction)

**Problem:** Chat message construction carries several pending buffers through an imperative loop. The state transitions are difficult to audit.

**Files involved:** `src/clients/chat_completions.rs`

**Fix:** Model message building as an explicit small state machine or well-named grouping passes. Preserve existing snapshots and add edge-case tests.

**Status:** Completed

**Completed:** 2026-05-11

**Notes:** Refactored `build_messages` into a private `ChatMessageBuilder` with named transitions for messages, tool calls, tool outputs, reasoning, developer context folding, and final flushing. Added edge-case tests for pending tool-call flushing before a user message and developer context preservation until the next user message. Existing Chat Completions snapshots stayed stable, and final `just ci` passed.
