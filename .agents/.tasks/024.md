# Revisit Retryable HTTP Status Codes

**Source:** src/clients/agent.rs review

**Problem:** `is_retryable_status()` retries `429`, `500`, and `503`, but excludes common transient gateway statuses such as `502 Bad Gateway` and `504 Gateway Timeout`.

**Files involved:**
- `src/clients/agent.rs:35`

**Fix:** Decide whether `502` and `504` should be treated as retryable for supported providers. If yes, update `is_retryable_status()` and add tests.

**Status:** Completed
