# Fix README Default Model Mismatch

**Source:** fix.md #1

**Problem:** README references `glm-5` as the default model in three places (lines 37, 189, 222), but `src/config/defaults.rs` defines `DEFAULT_MODEL` as `"glm-5.1"`.

**Files involved:**
- `README.md` (lines 37, 189, 222)
- `src/config/defaults.rs` (line 2)

**Fix:** Update all three occurrences of `glm-5` in `README.md` to `glm-5.1`. Alternatively, add a CI test that asserts the default model string from `defaults.rs` appears in the README.

**Status:** Completed
