# Use a Shared Duration Formatting Helper

**Source:** prompt.md item 17

**Priority:** P3

**Effort:** S

**Problem:** `format_seconds_tenths` is duplicated in `main.rs` and `bash.rs`.

**Files involved:** `src/main.rs`, `src/clients/tools/bash.rs`

**Fix:** Move the helper into a small shared module or keep one local copy if the usages diverge. Add tests around rounding.

**Status:** Pending
