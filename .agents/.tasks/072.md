# Add Size Limit or Streaming Plan for Stdin Input

**Source:** prompt.md item 63

**Priority:** P2

**Effort:** M

**Problem:** `read_stdin_content` reads all piped stdin into memory before the runtime starts. Large input can consume excessive memory.

**Files involved:** `src/main.rs`, stdin tests

**Fix:** Add a documented size cap with a clear error, or design streaming/temporary-file handling for large stdin content.

**Status:** Pending
