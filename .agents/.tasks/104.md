# Add Structured Provider Header Configuration

**Source:** prompt.md item 12

**Priority:** P3

**Effort:** M

**Depends on:** 050 (Backend abstraction), 052 (Provider strategy)

**Problem:** OpenRouter headers are hardcoded into request builders instead of being driven by provider or settings configuration.

**Files involved:** `src/clients/chat_completions.rs`, `src/clients/responses.rs`, settings model

**Fix:** Add provider/header configuration to model settings or provider strategy. Ensure headers are sent only when appropriate.

**Status:** Pending
