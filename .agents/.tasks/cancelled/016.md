---
id: 016
title: Refactor send() and execute_bash() into Smaller Functions
status: Cancelled
priority: P3
effort: M
labels: type:task, area:agent
exec_plan: -
depends_on: -
updated: 2026-07-07T13:29:58-04:00
---
# Refactor send() and execute_bash() into Smaller Functions

## Blocker
`send()` was already split into an agent loop module (tasks 049, 054). `execute_bash()` may still be long (~200+ lines). Needs audit to determine what work remains, then retitle/descope to cover only what is still relevant.

# Refactor send() and execute_bash() into Smaller Functions

**Source:** clippy-issues.md TODO #6

**Problem:** Both functions are long because they orchestrate complex workflows (agent loop and bash execution with sandboxing). They have `#[allow(clippy::too_many_lines)]` suppressions.

**Files involved:**
- `src/clients/agent.rs:261` on `send()`
- `src/clients/tools/bash.rs:258` on `execute_bash()`

**Fix:** Extract the tool execution loop in `send()` into a separate method. Extract bash execution phases into helpers. Remove the `too_many_lines` suppressions once refactored.

**Status:** Blocked

## Cancellation Reason

The send() half was completed via tasks 049/054. The remaining execute_bash split is a lint-suppression-driven refactor with no behavior value; bash.rs will be restructured by future command-policy work anyway.

