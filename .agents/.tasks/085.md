# Make Human-Readable Size Formatting Less Clever

**Source:** prompt.md item 18

**Priority:** P3

**Effort:** S

**Problem:** Size and duration formatting use integer arithmetic patterns that are easy to misread and can overflow on smaller targets.

**Files involved:** `src/clients/tools/bash.rs`, `src/main.rs`

**Fix:** Use wider integer types or clearer formatting functions. Remove misleading precision specifiers applied to already-formatted strings.

**Notes after 084:** Task 084 moved duration formatting into `src/time_format.rs`, removing the old `Duration::as_millis()` to `u64` conversion in `src/main.rs`. When working this task, inspect the new helper for readability, but the main remaining issues are in `src/clients/tools/bash.rs`: `format_kib_tenths` still uses compact integer arithmetic and binary-output strings apply `{:.1}` precision to an already-formatted string.

**Status:** Completed

**Completion notes:** Clarified the shared duration helper from task 084 with named constants and saturating rounding. Reworked Bash KiB formatting to use wider arithmetic with named constants, and removed misleading precision specifiers from binary-output messages.
