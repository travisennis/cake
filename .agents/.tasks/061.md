# Consolidate Conversation Serialization Paths

**Source:** prompt.md item 56

**Priority:** P1

**Effort:** XL

**ExecPlan:** `.agents/exec-plans/completed/consolidate-conversation-serialization.md`

**Problem:** `ConversationItem` is serde-serializable for sessions and also has hand-built JSON conversions for API input and stream output. These paths can drift.

**Files involved:** `src/clients/types.rs`, backend modules, session and stream tests

**Fix:** Define separate wire/session/API DTOs or centralized conversion traits. Add snapshot tests for each supported external representation.

**Status:** Completed

**Completed:** 2026-05-10

**Acceptance notes:** Responses API input now uses typed serializable DTOs, production stream JSON snapshots serialize `StreamRecord::from_conversation_item`, session JSON snapshots serialize the persisted `SessionRecord` path, and the dead streaming JSON helpers were removed. Final `just ci` passed on 2026-05-10.
