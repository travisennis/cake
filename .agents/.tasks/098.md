# Reorganize Type Modules

**Source:** prompt.md item 76

**Priority:** P3

**Effort:** XL

**Depends on:** 049 (Split Agent), 050 (Backend abstraction), 051 (Tool abstraction), 058 (Typed reasoning content), 061 (Consolidate serialization), 062 (DateTime types), 063 (Optional field normalization). **Do this last.**

**Problem:** Core types are spread across `models`, `clients::types`, `config::model`, and `clients::chat_types` without a clear boundary.

**Files involved:** `src/models`, `src/clients/types.rs`, `src/clients/chat_types.rs`, `src/config/model.rs`

**Fix:** Design a coherent type module layout and migrate gradually. Avoid churn until higher-priority behavior changes are covered by tests.

**Status:** Pending
