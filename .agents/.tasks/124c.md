# Replace Module-Wide `string_slice` Suppression in bash_safety.rs

**Status:** Pending
**Priority:** P1
**Effort:** S
**Created:** 2026-05-14
**Parent:** 124

## Summary

`src/clients/tools/bash_safety.rs` has a top-of-file `#![expect(clippy::string_slice, reason = "all string indexing operates on ASCII byte boundaries derived from prior byte-level iteration")]`. Replace with localized per-site `#[expect]` attributes.

## Files Involved

- `src/clients/tools/bash_safety.rs`

## Fix Direction

1. Remove the module-level `#![expect(clippy::string_slice, ...)]`.
2. For each slice site, either:
   - Refactor to a UTF-8-safe form, or
   - Wrap in a narrow `#[expect(clippy::string_slice, reason = "...")]` whose reason ties to the specific source of the index (e.g. "index produced by tokenizer that only advances over ASCII shell metacharacters").
3. Confirm `just clippy-strict` is clean.

## Acceptance Notes

- Module-level attribute removed.
- Each remaining string slice is justified locally or replaced.
- `just ci` passes.
