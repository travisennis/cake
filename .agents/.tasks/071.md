# Replace looks_like_uuid with Parser-Based Validation

**Source:** prompt.md item 11

**Priority:** P2

**Effort:** S

**Problem:** Session references are validated with a UUID-shaped string heuristic instead of `Uuid::parse_str`.

**Files involved:** `src/config/data_dir.rs`, `src/main.rs`

**Fix:** Parse UUIDs at the CLI/session-mode boundary and pass typed UUIDs into session loading. Update invalid-reference tests.

**Status:** Completed
