# Move Provider Quirks Behind Provider Strategy

**Source:** prompt.md item 12

**Priority:** P1

**Effort:** L

**Depends on:** 050 (Backend abstraction)

**Problem:** Provider-specific behavior such as Kimi reasoning placeholders and OpenRouter headers is embedded directly in request construction. Headers are sent regardless of provider.

**Files involved:** `src/clients/chat_completions.rs`, `src/clients/responses.rs`, config model types

**Fix:** Add provider strategy hooks for headers, message transforms, and compatibility workarounds. Apply OpenRouter headers only when configured.

**Status:** Pending
