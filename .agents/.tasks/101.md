# Review Module Size and Ownership Boundaries

**Source:** prompt.md module size critique

**Priority:** P3

**Effort:** XL

**Depends on:** 047 (ToolContext), 049 (Split Agent), 050 (Backend abstraction), 051 (Tool abstraction), 052 (Provider strategy), 053 (Typed SessionMode), 054 (Refactor run), 064 (build_messages refactor). **Do this after behavior-oriented refactors land.**

**Problem:** Several files are very large, especially `agent.rs`, `chat_completions.rs`, `settings.rs`, `main.rs`, `types.rs`, `responses.rs`, and `bash.rs`. This makes ownership and review harder.

**Files involved:** large Rust modules across `src/`

**Fix:** After behavior-oriented refactors land, split large modules by responsibility with minimal public API churn and strong tests.

**Status:** Pending
