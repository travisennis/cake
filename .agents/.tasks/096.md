# Make Git Branch Delete Detection Parser Safe

**Source:** prompt.md item 36

**Priority:** P3

**Effort:** M

**Problem:** `check_git_branch_delete` walks indexes in a lowercased string and maps them back to the original normalized string, relying on ASCII-only alignment.

**Files involved:** `src/clients/tools/bash_safety.rs`

**Fix:** Use tokenization or parser-backed matching instead of byte-index mapping between transformed strings.

**Status:** Pending
