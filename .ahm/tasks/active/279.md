---
id: 279
title: Deduplicate the full verification gate between handoff guidance and pre-push hook
status: Pending
priority: P2
effort: M
labels: type:task,area:build
exec_plan: -
depends_on: -
created: 2026-07-19T12:51:19-04:00
updated: 2026-07-26T21:28:42-04:00
---
# Deduplicate the full verification gate between handoff guidance and pre-push hook

## Context

`CONTRIBUTING.md` (line ~157) requires running `just ci` "before final handoff and before pushing", while the pre-push hook (`CONTRIBUTING.md` line ~29, `just pre-push`) already runs `just ci` mechanically. The same full gate is therefore demanded at two boundaries, and the handoff copy is enforced only by prose.

`just ci` is heavy (justfile line ~85): `rust-version-check check-linux fmt-check clippy-strict clippy-no-default-features test-all-features check-coverage lint-imports lint-module-size` — two full clippy passes, all-features tests, and an llvm-cov instrumented coverage build with the cargo-crap regression check. That is a multi-minute worker feedback loop paid on every agent handoff, even for small diffs.

## Work

1. Time `just ci` cold and warm; record the numbers in this task.
2. Decide the per-handoff gate policy: keep the full gate, or scale the handoff requirement to the narrowest checks matching the diff (e.g. fmt + clippy + affected tests) while the pre-push hook retains the full gate.
3. Update the authoritative verification policy in `CONTRIBUTING.md` and remove
   any duplicate instruction.

## Verification

This is a behavior-shaping instruction change. Record the motivating evidence
and expected behavior change — shorter handoff loops without more red pushes —
then run or explicitly defer a fresh agent-session probe.

## Acceptance criteria

- Measured `just ci` durations recorded.
- One documented gate policy with a single authoritative home; no contradictory
  statements between `CONTRIBUTING.md`, agent instructions, and hooks.

## Comments

**2026-07-20T08:56:39-04:00** — Ready for work. Clear acceptance criteria: time just ci, decide per-handoff gate policy, update CONTRIBUTING.md and guardrails with the single authoritative gate rule.

**2026-07-26T21:28:42-04:00** — Supersedes the routing in the 2026-07-20 comment. The gate policy is contributor workflow and its single authoritative home is CONTRIBUTING.md, matching Work item 3; do not also put it in docs/guardrails/. That directory was removed by c16b0e0 and restored in 72b69ed scoped to rules that shape agent behavior, per the durable-prose taxonomy in AGENTS.md. The Verification section of this task is unchanged and still applies: this remains a behavior-shaping instruction change needing motivating evidence, an expected behavior change, and a run or explicitly deferred fresh-session probe.

