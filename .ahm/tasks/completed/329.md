---
id: 329
title: Refactor is_input_error in Exit Code Module
status: Completed
priority: P2
effort: S
labels: type:refactor,area:exit_code
exec_plan: -
depends_on: 325
created: 2026-07-30T13:51:13-04:00
updated: 2026-07-31T15:34:18-04:00
---
# Refactor is_input_error in Exit Code Module

## Summary

Reduce cyclomatic complexity of `is_input_error` (CC 27) in
`src/exit_code.rs:140`. This function matches error variants to classify
user-input vs internal failures.

## Approach

1. Write focused tests for `is_input_error` covering every error variant
   (≥ 80% line + branch).
2. Run `cargo-crap --lcov lcov.info` to confirm CRAP drop.
3. Refactor: the CC is inherently dispatch-heavy — consider a typed
   marker trait or enum on the error types themselves rather than a
   central match; if structural, accept CC ≤ 15 with ≥ 90% coverage.
4. Verify targets.

## Acceptance Notes

- [x] Function has 100% line coverage; a focused nightly branch-coverage
      run covered both outcomes of the instrumented branch
- [x] CC reduced from 27 to 2
- [x] `cargo-crap` reports CRAP 2 at 100% coverage
- [x] `just ci` passes

The existing input-error strings and compound matching semantics are preserved
in declarative pattern tables, with focused positive and partial-pattern
negative tests. Independent review and preflight reported no findings.

Documentation impact was assessed against `docs/integrations.md` and
`docs/guardrails/complexity-targets.md`; no update is needed because this
refactor preserves the exit-code contract and existing quality targets.

