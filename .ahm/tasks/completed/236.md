---
id: 236
title: Replace Duplicate-Mutation Guard with Per-Path Serialization
status: Completed
priority: P2
effort: M
labels: type:refactor, area:agent, area:tools
exec_plan: -
depends_on: -
created: 2026-07-07T13:37:13-04:00
updated: 2026-07-09T07:58:31-04:00
---
# Replace Duplicate-Mutation Guard with Per-Path Serialization

**Created:** 2026-07-07
**Source:** 2026-07-07 backlog triage (bitter-lesson audit V2 / Phase 2 item 7).

## Problem

Tool calls in one assistant turn run concurrently via `join_all`. Rather than fix the resulting same-file race, the harness rejects the second same-file Edit/Write (`src/clients/tools/duplicate_guard.rs`, ~394 lines) and burns an API round-trip telling the model to re-read. The workaround is then echoed in three prompt layers: `src/prompts/system.md` ("Do not issue multiple Edit or Write calls for the same file in one turn"), `edit-description.txt`, and `write-description.txt` — four encodings of one implementation shortcut, and a model that correctly batches two sequential edits to one file gets punished.

Decision (2026-07-07): fix the race with per-path serialization. This is deliberately narrower than cancelled task 202 (which proposed serializing all tool calls, including Bash, and was rejected as over-complicated): serializing only same-path mutations fixes the harness's own concurrency choice without constraining model judgment.

## Fix Direction

- In the agent loop scheduling (`src/clients/agent/agent_loop.rs`), group scheduled mutating calls by canonical target path (the existing `mutating_target` extraction). Groups run concurrently with each other and with non-mutating calls; members within a group run sequentially in issue order, each seeing the previous call's effects.
- Delete the rejection path in `duplicate_guard.rs`; keep/relocate only the canonical-path extraction the scheduler needs.
- Delete the three prompt-layer echoes (system.md line, edit-description.txt, write-description.txt). Note: task 164 added some of this guidance; it is intentionally reverted by this change.
- Tool results must preserve per-call attribution and ordering in session records and stream-json output.
- This changes tool execution semantics — a documented compatibility surface. Update `docs/design-docs/tools.md` and `docs/guardrails/agent-loop-tools-and-tool-execution.md`; assess whether an ADR is needed (likely yes, short one, since execution semantics are a durable contract).
- Optionally count serialization events (calls that were reordered/sequenced) in session telemetry so the compensation is measured (see the telemetry-counters task).

## Relevant Files

- `src/clients/agent/agent_loop.rs` — scheduling and `join_all`
- `src/clients/tools/duplicate_guard.rs` — rejection logic to remove
- `src/clients/tools/mod.rs` — `mutating_target` plumbing
- `src/prompts/system.md`, `src/clients/tools/edit-description.txt`, `src/clients/tools/write-description.txt` — echoes to delete
- Duplicate-guard tests (task 188 added coverage) — rewrite as serialization tests
- `docs/design-docs/tools.md`, `docs/guardrails/agent-loop-tools-and-tool-execution.md`

## Implementation Notes (2026-07-09)

- ADR-013 (`docs/adr/013-per-path-serialization-of-mutating-tool-calls.md`, accepted) records the execution-semantics decision.
- `src/clients/tools/duplicate_guard.rs` deleted; replaced by `src/clients/tools/scheduling.rs`, which partitions a turn's tool plans into groups by canonical mutating target path (`ToolRegistry::mutating_target`, unchanged). `Agent::run_tool_plans` runs groups concurrently, group members sequentially in issue order, then re-sorts results to issue order before recording/streaming.
- Hook-blocked decision: `Block` plans carry no executable arguments, so they never join a serialization group; they resolve to immediate error results and neither delay nor abort the executable same-path mutation (matches prior guard behavior; covered by `hook_blocked_call_is_not_serialized_with_same_path_mutation`). A failed call within a group does not abort later members — each runs against the state prior calls left.
- Prompt echoes removed from `system.md`, `edit-description.txt`, `write-description.txt` (intentional revert of task 164 guidance); 7 insta snapshots regenerated.
- Docs: new "Tool-Call Scheduling" section in `docs/design-docs/tools.md`, new compatibility-surface bullet in `docs/guardrails/agent-loop-tools-and-tool-execution.md`, `tools` code-map entry in `ARCHITECTURE.md` updated.
- Serialization-event telemetry deliberately not added here; it belongs to the telemetry-counters task.
- Preflight (2026-07-09): CRAP baseline regenerated via `just change-risk-baseline` (per justfile guidance for intentional coverage/complexity changes) — `duplicate_guard` entries removed, `scheduling.rs` entries added; report shows 0 regressed/0 new against current code. `classify_tool_error` in `scripts/session-metrics/cakelib.py` keeps its "duplicate-mutation guard" bucket intentionally: historical session files still contain those rejection outputs.

## Acceptance Notes

- [x] Two Edits targeting the same file in one turn both execute, in issue order, with the second operating on the first's result. Verified by `same_file_edits_in_one_turn_run_sequentially_in_issue_order` (agent_tests.rs), where the second edit's `old_text` only exists after the first edit applies.
- [x] Mutations to different files still run concurrently; non-mutating calls are unaffected. Verified by `scheduling.rs` unit tests (`mutations_to_different_files_stay_concurrent`, `repeated_reads_for_same_file_stay_concurrent`, `interleaved_calls_group_by_path_and_keep_issue_order`).
- [x] No rejection round-trip remains; prompt echoes are removed and prompt snapshots updated (7 snapshots regenerated via `just snapshots`, accepted with `cargo insta accept`).
- [x] Session records and stream-json preserve call ordering and attribution: results are re-sorted to issue order before `record_tool_results`; the e2e test asserts output order and per-call ids.
- [x] Hook-blocked calls within a serialized group behave sanely: blocked calls never join a group and do not delay or abort the executable same-path mutation (tested).
- [x] `just ci` passes (2026-07-09).

