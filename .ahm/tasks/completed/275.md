---
id: 275
title: Graceful fallback when nested inside another Seatbelt sandbox
status: Completed
priority: P2
effort: S
labels: type:feature, area:sandbox, risk:security-sensitive
exec_plan: -
depends_on: -
created: 2026-07-13T20:29:33-04:00
updated: 2026-07-13T20:53:04-04:00
---
# Graceful fallback when nested inside another Seatbelt sandbox

# Graceful Fallback When Nested Inside Another Seatbelt Sandbox

## Problem

cake uses `sandbox-exec` (macOS Seatbelt) to sandbox every Bash tool invocation. When cake itself is already running inside another Seatbelt sandbox (e.g. inside OpenAI Codex CLI, Claude Code, or any other tool that uses `sandbox-exec`), macOS rejects the nested `sandbox_apply` syscall with EPERM. Currently, `detect_macos_platform()` treats any sandbox probe failure as a hard error and blocks all Bash commands. This prevents cake from functioning as a subagent inside any other sandboxed AI coding assistant.

## Relevant Files

- `src/clients/tools/sandbox/mod.rs` — `detect_macos_platform()` at lines 714-742 returns `Err` when profile can't be applied.
- `src/clients/tools/sandbox/macos.rs` — `probe_can_apply_profile()` at line 46 runs the sandbox-exec probe; `can_apply_profile()` caches and returns the result.
- `src/clients/tools/bash.rs` — `execute_bash_with_args()` at line 334 propagates the `Err` via `?`.

## Fix Direction

In `detect_macos_platform()`, distinguish the nested-sandbox case (stderr contains `sandbox_apply: Operation not permitted`) from other probe failures. When the probe fails specifically because we're already inside another sandbox, return `Ok(None)` with a `tracing::warn!` instead of returning `Err`. This allows the Bash tool to run commands unsandboxed — the outer sandbox already constrains the process tree, so cake's sandbox would be redundant.

The outer sandbox still protects the host, but cake cannot independently guarantee that its path policy matches the inherited policy. ADR-016 records that tradeoff.

## Architectural Decision

- `docs/adr/016-nested-seatbelt-sandbox-fallback.md` records the accepted fallback boundary and its security tradeoff.

## Acceptance Notes

- [x] `detect_macos_platform()` returns `Ok(None)` when the probe failure message contains `sandbox_apply: Operation not permitted`.
- [x] Other probe failures (missing `sandbox-exec`, broken profile, etc.) still return `Err` and block execution.
- [x] A `tracing::warn!` message is emitted when falling back to unsandboxed execution.
- [x] The parent-independent failure-signature path lets nested `codex exec` run through cake's Bash tool without `--sandbox danger-full-access`; the exact signature is injected in tests because this host cannot reproduce a live nested run.
- [x] `claude` and other Seatbelt-using parents use the same generic fallback path.
- [x] Test: mock a nested-sandbox probe failure → expect `Ok(None)` and a warning log.
- [x] Test: mock a non-nested probe failure → expect `Err`.

## Verification

- `cargo test macos_detection_`
- `cargo test sandbox_initialization_failure`
- `CAKE_REQUIRE_SANDBOX_TESTS=1 cargo test --quiet -- test_sandbox_`
- `just ci`
- Scoped Panache format and lint checks for the changed durable documentation.
- The host can apply Seatbelt profiles normally, so live nested Codex/Claude invocation was not reproducible; the unit test injects the exact nested `sandbox-exec` failure signature and verifies the generic fallback path used by either parent.

## Preflight

- L-scale review covered repository rules, task/ADR/design conformance, sandbox source-of-truth behavior, and simplification.
- Fixed a downstream diagnostic regression by basing sandbox-initialization and restriction classification on whether cake actually applied a strategy, not merely whether the user requested sandboxing.
