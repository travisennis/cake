---
id: 247
title: Surface .agents/skills to Non-Cake Agents
status: Completed
priority: P2
effort: S
labels: type:task, area:dev-env
exec_plan: -
depends_on: -
created: 2026-07-07T13:37:29-04:00
updated: 2026-07-07T20:36:08-04:00
---
# Surface .agents/skills to Non-Cake Agents

**Created:** 2026-07-07
**Source:** 2026-07-07 harness assessment (feedback finding #3); maintainer decision 2026-07-07: the repo is worked by many coding agents (cake primary; also claude, codex, acai, amp), so harness knowledge must be agent-agnostic.

## Problem

`.agents/skills/` (analyzing-cake-sessions, debugging-sandbox, debugging-cake, grooming-backlog, preflight, finding-improvements, auditing-binary-size) is discovered only by cake's skill mechanism. AGENTS.md's routing table never mentions the directory, so every other agent working this repo misses playbooks written for exactly the situations it hits (e.g. debugging a Seatbelt denial with the debugging-sandbox skill sitting invisible next door).

## Fix Direction

Claude Code is the only agent that does not discover `.agents/skills/` natively (it looks in `.claude/skills/` instead). Cake, acai, codex, and amp all support `.agents/skills/` already. The simplest fix is a single committed symlink:

- Create `.claude/skills` → `../.agents/skills` as a relative symlink.
- No AGENTS.md changes needed — the symlink makes skills auto-discoverable by Claude Code.
- No SKILL.md changes needed — the existing YAML front-matter format (`name` + `description`) matches what Claude Code expects.

## Relevant Files

- `.claude/skills` — symlink target (does not exist yet, will point to `.agents/skills`)

## Acceptance Notes

- [x] `.claude/skills` is a relative symlink to `.agents/skills`.
- [x] Claude Code auto-discovers all seven skills when loaded in this repo.
- [x] Skills are not duplicated; `.agents/skills/` remains the canonical location.
- [x] `git status` shows only the new symlink (plus the task file edit).

