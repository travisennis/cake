---
id: 163
title: Prevent Concurrent Same-File Mutating Tool Calls
status: Completed
priority: P1
effort: L
labels: type:bug, area:agent, area:tools
exec_plan: .agents/exec-plans/completed/prevent-concurrent-same-file-mutations.md
depends_on: -
---
# Prevent Concurrent Same-File Mutating Tool Calls

**Status:** Completed
**Priority:** P1
**Effort:** L
**Created:** 2026-05-20
**Source:** Evaluation of session `a91aa043-4833-4118-88ff-92feef8688b9`
**ExecPlan:** `.agents/exec-plans/completed/prevent-concurrent-same-file-mutations.md`
**Depends on:** -

## Problem

Cake currently lets the model issue multiple mutating tool calls for the same file in one assistant turn. In session `a91aa043-4833-4118-88ff-92feef8688b9`, this appears to have caused lost edits and trailing file corruption while task 099 was rewriting doctests.

Current prompt and tool descriptions already tell the model not to issue multiple same-file `Edit` or `Write` calls in one turn. The remaining gap is enforcement in the agent/tool execution path; guidance alone does not prevent concurrent same-file mutation.

Evidence:

- Lines 69-70: two `Edit` calls targeted `src/config/model.rs` in the same model turn.
- Lines 76-81: six `Edit` calls targeted `src/config/settings.rs` in the same model turn.
- Lines 104-109: six `Edit` calls targeted `src/config/data_dir.rs` in the same model turn.
- Line 134: `cargo check --tests` failed with `unexpected closing delimiter` in `src/config/model.rs`.
- Lines 170-179: the agent found and removed trailing garbage in `src/config/model.rs`.
- Lines 361-387: the same corruption pattern appeared in `src/config/settings.rs`.

The task eventually succeeded, but recovery consumed many turns and tool calls.

## Relevant Files

- `src/clients/agent.rs`
- `src/clients/tools/mod.rs`
- `src/clients/tools/edit.rs`
- `src/clients/tools/write.rs`
- Conversation/tool execution tests around parallel tool calls

## Fix Direction

Make mutating tool execution path-aware. Possible approaches:

- Serialize `Edit` and `Write` calls that target the same path within one assistant turn.
- Reject duplicate same-path mutating calls in the same turn with a clear tool result telling the model to re-read and edit sequentially.
- Keep parallel execution for reads and independent writes to different paths.

The implementation should preserve deterministic call/result ordering so persisted sessions remain understandable.

Because this changes agent-loop and tool-execution behavior, create an ExecPlan before implementation.

## Acceptance Notes

- Implemented same-turn duplicate mutation rejection after pre-tool hooks and before concurrent tool execution.
- `Edit` and `Write` expose canonical mutation targets through the tool registry; malformed target extraction falls through to the existing tool-specific validation errors.
- Rejected duplicate mutation outputs begin with `Error:` and tell the model another same-file mutation already appeared in the assistant turn, then instruct it to wait, re-read, and retry sequentially.
- Added tests for same-file `Edit`/`Edit`, same-file `Edit`/`Write`, relative-vs-absolute same-file detection, different-file mutations, and repeated non-mutating `Read` calls.
- Verification passed: `cargo check --tests`, `cargo test duplicate_guard`, `cargo fmt`, `just clippy-strict`, `just test`, `just lint-imports`, `just lint-module-size`, `just rust-version-check`, and `just coverage-check` (91.86% line coverage).
- `just ci` was attempted but is blocked at `task-index-check`: `ahm --dry-run index` still reports all generated indexes stale immediately after `ahm index` and `ahm --force index`.
- Deslop pass added the relative-vs-absolute canonical path test and reran focused validation.
