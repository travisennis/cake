---
id: 272
title: Cap provider Retry-After delays
status: Completed
priority: P2
effort: S
labels: type:task,area:clients
exec_plan: -
depends_on: -
created: 2026-07-13T18:35:18-04:00
updated: 2026-07-15T21:51:53-04:00
---
# Cap provider Retry-After delays

## Summary

At commit `6e69b45`, parseable `Retry-After` values bypass the retry policy's 30-second maximum backoff. A provider or intermediary can therefore stall the CLI for an arbitrarily large delta or far-future HTTP date, while fallback exponential delays are bounded.

## Evidence

- `src/clients/retry.rs:12-31` — the default retry policy defines `max_backoff` as 30 seconds.
- `src/clients/retry.rs:295-301` — retry classification uses `parse_retry_after(headers).unwrap_or_else(fallback_delay)` directly.
- `src/clients/retry.rs:344-365` — both delta seconds and future HTTP dates become `Duration` values without clamping to policy limits.
- `src/clients/retry.rs:527-555` — parser tests verify ordinary values but do not cover a delay beyond the configured maximum.
- `docs/design-docs/api-retry-strategy.md:28-30` says `Retry-After` wins when parseable and describes fallback backoff as bounded; it does not define an unlimited server-controlled wait.

## Fix Direction

Apply an explicit upper bound to header-derived retry delays, preferably through the existing retry policy so custom policy tests remain deterministic. Preserve header precedence for reasonable values, HTTP-date parsing, retry classification, callback status, and retry budget. Update the retry design doc if the cap is a newly explicit behavioral contract. If providers require honoring longer delays for a known compatibility reason, stop and surface that decision rather than retaining an unbounded foreground stall.

## Acceptance Notes

- [x] Large delta-second and far-future HTTP-date values are capped to the policy maximum. Cap applied via `.min(policy.max_backoff)` in `retry_status()` at `src/clients/retry.rs:296-298`. Tests `retry_after_delta_seconds_capped_to_policy_max_backoff` and `retry_after_http_date_capped_to_policy_max_backoff` verify with custom 5s/10s caps.
- [x] Ordinary `Retry-After` values below the cap are still honored exactly. Test `retry_after_below_cap_is_honored_exactly` verifies 3s delay with a 10s cap; existing `parse_retry_after_delta_seconds` and `parse_retry_after_http_date` unit tests also pass unchanged.
- [x] Fallback backoff, jitter, retry counts, and no-retry headers remain unchanged. `fallback_delay` is not touched; `capped_backoff_ms`, `deterministic_jitter_ms`, `parse_x_should_retry`, `classify_http_failure` retry-budget check at line 102, and `classify_transport_error` are all unchanged.
- [x] Retry status/progress reports the effective bounded delay. The `delay` field in `RetryStatus` is set from the capped value; `wait_for_retry` in `agent_runner.rs` logs and sleeps using `status.delay` directly — no separate code path.
- [x] `cargo test retry` passes. All 20 retry-filtered tests pass (0 failed).
- [x] `just ci` was run after preflight. Formatting, clippy, checks, all 1024 unit tests, integration tests, and the 91.49% coverage threshold pass. The command exits 1 only at the CRAP regression gate for `execute_bash_with_args` in unchanged `src/clients/tools/bash.rs` (CRAP 23.6, delta +2.7), outside this task's diff.

## Comments

**2026-07-15T21:51:53-04:00** — Review finding (P3): HTTP-date cap test used a fixed 2031-04-28 date that would expire. Fixed by constructing an HTTP date 300 seconds from the current time and exercising the production retry classifier, so the test remains non-expiring and directly guards the cap. No behavior change. Verified `cargo fmt --check` and `cargo test retry` pass.
